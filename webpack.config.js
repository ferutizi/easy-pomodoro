/* const path = require('path');

module.exports = {
  entry: './src/app/layout.tsx',  // Ajusta la entrada según tu estructura de archivos
  output: {
    filename: 'bundle.js',
    path: path.resolve(__dirname, 'dist'),
  },
  module: {
    rules: [
      {
        test: /\.(mp3|wav)$/,
        use: [
          {
            loader: 'file-loader',
          },
        ],
      },
    ],
  },
}; */